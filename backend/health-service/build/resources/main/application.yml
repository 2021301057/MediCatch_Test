spring:
  application:
    name: health-service
  datasource:
    url: jdbc:mysql://localhost:3306/medicatch_health?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 1234
    #    password: medicatch_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

server:
  port: 8003
  servlet:
    context-path: /

codef:
  api-url: https://api.codef.io/v1
  client-id: ${CODEF_CLIENT_ID:YOUR_CODEF_CLIENT_ID}
  client-secret: ${CODEF_CLIENT_SECRET:YOUR_CODEF_CLIENT_SECRET}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    instance-id: ${spring.application.name}:${random.value}
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 30

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized

logging:
  level:
    root: INFO
    com.medicatch: DEBUG
